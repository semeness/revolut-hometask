import { TAccount } from '../types/Account.interface';

export const euro: TAccount = { id: 0, name: 'EUR' };
export const usd: TAccount = { id: 1, name: 'USD' };
export const gbp: TAccount = { id: 2, name: 'GBP' };
